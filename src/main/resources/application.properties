spring.application.name=ForgetPassword-microService
server.port=8081

spring.datasource.url=jdbc:mysql://localhost:3306/forget_password?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=12345678

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Resend: create an API key at https://resend.com (API Keys). Leave empty to print OTP in the console.
# Until you verify a domain, Resend only delivers to the email you signed up with.
# Do not commit a real key.
resend.api-key=
resend.from=Marketplace <onboarding@resend.dev>
resend.url=https://api.resend.com/emails
